language: php

php:
  - 5.3
  - 5.4

before_script:
    - composer dump-autoload
    - pyrus install pear/PHP_CodeSniffer
    - phpenv rehash

script:
    - phpunit -c phpunit.xml --coverage-text tests/
    - phpcs --ignore=vendor/* --standard=PSR2 -nsp --encoding=UTF-8 ./
    